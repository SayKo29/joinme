import { View, Text } from "react-native";
import React from "react";

const ForYouEvent = ({ data, users }) => {
    return (
        <View>
            <Text>ForYouEvent</Text>
        </View>
    );
};

export default ForYouEvent;
